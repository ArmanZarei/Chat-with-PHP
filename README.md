# Chat-with-PHP

- Start the project 
    ```Bash
    > make up
    ```
- Stop the project
    ```Bash
    > make down
    ```
    
Navigate to http://localhost:8080/ (or http://your-local-ip:8080/ for local usage)
